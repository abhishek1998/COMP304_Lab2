<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.araturi.abhishekraturi_comp304_lab2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TownHouse"></activity>
        <activity android:name=".SemiDetachedHome" />
        <activity android:name=".DetachedHome" />
        <activity android:name=".Condominium" />
        <activity android:name=".Apartments" />
        <activity android:name=".Payment" />
        <activity android:name=".SecondScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>